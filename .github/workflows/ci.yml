name: Teneria ERP CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: teneria
          POSTGRES_PASSWORD: teneria_dev
          POSTGRES_DB: teneria
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U teneria -d teneria"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10
    env:
      DATABASE_URL: postgresql://teneria:teneria_dev@localhost:5432/teneria?schema=public
      AUTH_SECRET: ci-only-secret-value-with-at-least-32-characters
      AUTH_BOOTSTRAP_EMAIL: admin@teneria.local
      AUTH_BOOTSTRAP_PASSWORD: ci-only-password-12345
      AUTH_BOOTSTRAP_NAME: CI Administrator
      DOCUMENT_STORAGE_PATH: /tmp/teneria-ci-documents
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        run: npm install
      - name: Validate operations scripts
        run: |
          bash -n scripts/backup-teneria.sh
          bash -n scripts/restore-teneria.sh
          bash -n scripts/deploy-production.sh
          bash -n scripts/health-watch.sh
      - name: Validate Prisma
        run: |
          set -o pipefail
          npx prisma validate 2>&1 | tee prisma-validate.log
      - name: Generate Prisma client
        if: success()
        run: npx prisma generate
      - name: TypeScript
        if: success()
        run: |
          set -o pipefail
          npx tsc --noEmit 2>&1 | tee typescript.log
      - name: Prepare integration database
        if: success()
        run: |
          npx prisma db push --skip-generate
          npm run db:seed
      - name: PostgreSQL integration smoke
        if: success()
        run: |
          set -o pipefail
          npm run test:integration 2>&1 | tee integration.log
      - name: Shipment concurrency smoke
        if: success()
        run: |
          set -o pipefail
          npx tsx scripts/shipment-concurrency-smoke.ts 2>&1 | tee shipment-integration.log
      - name: Shipment cancellation smoke
        if: success()
        run: |
          set -o pipefail
          npx tsx scripts/shipment-cancel-smoke.ts 2>&1 | tee shipment-cancel-integration.log
      - name: Finance reversal smoke
        if: success()
        run: |
          set -o pipefail
          npx tsx scripts/finance-reversal-smoke.ts 2>&1 | tee finance-reversal-integration.log
      - name: Quality rework smoke
        if: success()
        run: |
          set -o pipefail
          npx tsx scripts/quality-rework-smoke.ts 2>&1 | tee quality-rework-integration.log
      - name: Production build
        if: success()
        run: |
          set -o pipefail
          npm run build 2>&1 | tee build.log
      - name: Upload validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-logs-${{ github.run_number }}
          path: |
            prisma-validate.log
            typescript.log
            integration.log
            shipment-integration.log
            shipment-cancel-integration.log
            finance-reversal-integration.log
            quality-rework-integration.log
            build.log
          if-no-files-found: ignore
